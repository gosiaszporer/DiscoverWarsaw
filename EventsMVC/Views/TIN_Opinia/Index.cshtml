@model IEnumerable<EventsMVC.Models.TIN_Opinia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tresc)
        </th>
        <th>
            Wydarzenie
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tresc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIN_Wydarzenie.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TIN_Opinia_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TIN_Opinia_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TIN_Opinia_ID })
        </td>
    </tr>
}

</table>
